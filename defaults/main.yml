---

# Implemented
download_epel: true
download_el: false


# To be implemented:

# Distributions other than centos

# Add a mounted repository which is copied into the permanent hosted platfrom
#repository_mounted_path: "/mnt/repo"

# Change which application to host the repositories (ftp, http, https?)
#protocol: "http"


# Add to client!
#epel_repositories:
#- epel:
#    shortname: epel
#    name: Extra Packages for Enterprise Linux 7 - $basearch
#    baseurl: 
#    gpgkey: file:///etc/pki/rpm-gpg/RPM-GPG-KEY-EPEL-7
#    gpgcheck: true

